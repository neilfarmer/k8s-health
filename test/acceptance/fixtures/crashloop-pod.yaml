apiVersion: v1
kind: Pod
metadata:
  name: crashloop-test
  namespace: test-fixtures
spec:
  containers:
    - name: crash
      image: busybox:latest
      command: ["sh", "-c", "exit 1"]
  restartPolicy: Always
