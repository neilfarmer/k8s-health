apiVersion: v1
kind: Pod
metadata:
  name: imagepull-test
  namespace: test-fixtures
spec:
  containers:
    - name: bad-image
      image: this-image-does-not-exist-anywhere:v999
      imagePullPolicy: Always
