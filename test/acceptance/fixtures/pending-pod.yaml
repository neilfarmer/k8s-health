apiVersion: v1
kind: Pod
metadata:
  name: pending-test
  namespace: test-fixtures
spec:
  containers:
    - name: app
      image: busybox:latest
      command: ["sleep", "3600"]
      resources:
        requests:
          cpu: "100"
